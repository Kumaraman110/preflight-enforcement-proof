{
  "_comment_hooks": "Preflight accountability-layer hook registration. The installer MERGES this 'hooks' block into the consumer's .claude/settings.json (preserving any existing 'permissions' / other keys — never overwrite the file). These are first-class settings-based hooks: they fire WITHOUT plugin enablement. Every command resolves via ${CLAUDE_PROJECT_DIR} anchored on the consumer's installed .claude/hooks/ tree (the unified framework root — skills/agents are platform-locked to .claude/, so hooks+lib+examples join them there; .preflight/ holds runtime state only) — there is NO dependency on ${CLAUDE_PLUGIN_ROOT} (which is empty off-plugin and never reaches sub-agents). The dispatcher (run-hook.cmd) self-resolves its own location; the hook scripts self-resolve via $BASH_SOURCE. Keep the 'hooks' value below byte-identical to hooks/hooks.json — same events, matchers, and command shape — so plugin-form and settings-form registration never drift.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" session-start",
            "timeout": 5000,
            "async": false
          },
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" drift-detector",
            "timeout": 10000,
            "async": true
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" pre-push-gate-check \"$TOOL_INPUT\"",
            "timeout": 10000,
            "async": false
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" bootstrap-write-gate \"$TOOL_INPUT\"",
            "timeout": 5000,
            "async": false
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" coupled-edit-gate \"$TOOL_INPUT\"",
            "timeout": 5000,
            "async": false
          },
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" bootstrap-write-gate \"$TOOL_INPUT\"",
            "timeout": 5000,
            "async": false
          }
        ]
      },
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR}/.claude/hooks/run-hook.cmd\" rubric-validity-gate \"$TOOL_INPUT\"",
            "timeout": 5000,
            "async": false
          }
        ]
      }
    ]
  }
}
