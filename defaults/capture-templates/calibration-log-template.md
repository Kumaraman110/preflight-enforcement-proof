# Calibration Log

Entries here record cases where Stage 1's detection signal was too weak (missed something the rubric covers) or severity was miscalibrated. Used by batched rubric-edit PRs to strengthen detection signals.

---

