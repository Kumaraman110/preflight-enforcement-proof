# Machine-local cached state (drift detector, derived state)
.preflight/cache/
.preflight/derived/

# Gate evidence and metrics (local operational state)
.preflight/gate/
.preflight/metrics.json

# Migration checkpoint (session-local recovery state)
.preflight/migrate-checkpoint.json

# Graphify output (external comprehension tool)
graphify-out/
